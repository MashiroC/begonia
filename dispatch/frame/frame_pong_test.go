package frame

import (
	"fmt"
	"testing"
)

func TestPong_Opcode(t *testing.T) {
	/*m:=map[string]string{"cpu":"3"}
	pong := NewPong(m, nil)
	opcode := pong.Opcode()
	fmt.Println(int(opcode))
	fmt.Println(ParseOpcode(opcode))*/
	var a []int
	a = append(a, 3)
	fmt.Println(a)
}
